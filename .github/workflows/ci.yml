name: ci

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  ci:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [14]

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@master

      - name: Setup node env 🏗
        uses: actions/setup-node@v2.1.5
        with:
          node-version: ${{ matrix.node }}
          check-latest: true

      - name: Cache node_modules 📦
        uses: actions/cache@v2.1.4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies 👨🏻‍💻
        # run: npm install
        run: npm ci --prefer-offline --no-audit

      # - name: Run linter 👀
      #   run: npm run lint

      # - name: Run tests 🧪
      #   run: npm run test

      # - name: Run Synk Security 👨‍💻

      #   DEV ONLY
      - name: Run Vite Devs 🧱
        run: npm run dev

      # - name: Run Vite Build 🧱
      #   run: npm run build

      # - name: Run Vite Preview 🏛️
      #   run: npm run preview

      # - name: Run Deployment 📊
      # - name: Run Monitoring 🚀
